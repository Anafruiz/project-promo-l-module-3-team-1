{
  "name": "team-1-api",
  "version": "1.0.0",
  "description": "project",
  "scripts": {
    "start": "cd server && npm start",
    "build": "npm install",
    "dev": "cd web && npm start & cd server && npm run dev",
    //este comando tengo que ejecutarlo cada vez que modifique algo y quiera actualizarlo en HEROKU
     "docs": "react-scripts build && rm -rf ../server/public/ && mv build/ ../server/public/",
    "install": "cd web && npm install && cd ../server && npm install"
  },
  "author": "team1",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "prop-types": "^15.7.2"
  }
}
